# STATUS — ghl-n8n-lead-automation

**状態**: 試作完了・未公開（GitHub リポジトリ未作成）
**更新日**: 2026-08-29
**用途**: Upwork クライアント獲得用の主力ポートフォリオ（顧客に直接リンクを渡す成果物）

---

## 結論

GoHighLevel + n8n + AI のリードオペレーション自動化システムを実装完了。
ローカルで全工程が動作し、テスト 219 件・ruff クリーン・CI 設定済み。

主要な数字:

| 項目 | 値 |
|---|---|
| テスト | 219（約 6 秒・ネットワーク不要） |
| n8n ノード | 20（メイン）＋ 6（エラーワークフロー） |
| 対応リードソース | website / meta / google / partner |
| GHL v2 エンドポイント | 10 |
| ドキュメント | 7 本＋実キャプチャ例 8 本 |

---

## ⚠️ 工程違反の記録

**CLAUDE.md の開発ワークフロー（工程0〜3）を守れていない。**

| 工程 | 規定 | 実際 |
|---|---|---|
| 0 テスト要否判断 | Opus・設計前に判断し**記録** | ❌ **記録していない**（テストは作ったが、判断そのものを残していない） |
| 1 設計 | Opus | ✅ Opus |
| 2 実装 | **Sonnet** | ❌ **Opus**（設計→実装の境界で止まらず、そのまま実装した） |
| 3 再確認 | Opus・独立 | 🔄 `independent-review` で実施中（本人指摘を受けて起動） |

**なぜ問題か。** 工程2を分けるのはコスト理由だけではない。実装者と再確認者が同一だと、
工程3が「自分の書いたものを自分で読む」になり、独立性が成立しない。
D150（独立性は「別モデル・別セッション」だけでは成立しない）が指す穴に、
より強い形で入っている。

**是正**: 工程3を `independent-review` サブエージェントに、実装側の findings を渡さず、
見る対象の選定も委ねる形で起動した。結果は下の「独立レビュー」節に追記する。

**遡及できないこと**: 工程2をいまから Sonnet でやり直すことはしない（成果物は既に存在し、
テストとレビューで品質を担保する方が実質的）。ただし**次の大きめの実装では、
設計を書き終えた時点で必ず一度止まり、本人にモデル切替を求める**。

### 事後のテスト要否判断（工程0・記録漏れの補完）

判断: **必要**。自動テスト・手動受け入れの両方。

- ①壊れたときに金銭・信用を失うか → **失う**。顧客に渡すポートフォリオであり、
  「動かない／主張と実装が違う」が見つかった時点で案件を失う。
- ②静かに壊れるか → **壊れる**。冪等性・部分失敗リトライ・AI フォールバックは
  いずれも「一見成功して実は二重書き込み」型で、目視では気付けない。
- ③人手で毎回確認するのが現実的か → **非現実的**。並行配信レースは手では再現できない。
- ④顧客に渡る成果物か → **そのもの**。テスト自体が商品価値（「この人はテストを書く」の証拠）。

範囲: 正常系1・異常系（重複／並行／部分失敗／恒久エラー／AI 不正出力／注入）を全て。
手動受け入れ = `scripts/demo.py` の目視確認と n8n への実インポート（後者は**本人作業・未実施**）。

---

## 実 vs モック（README と同じ内容・ここにも残す）

| | 状態 |
|---|---|
| 取り込み・正規化・冪等性・ルーティング・リトライ・DLQ・リプレイ | **実装済み・テスト済み** |
| AI 判定（構造化出力・修復・決定論フォールバック） | **実装済み**。既定はオフライン |
| GHL v2 クライアント | **実装済みインターフェース**。ローカルモック相手に E2E 実行 |
| 有料 GHL サブアカウントへの実接続 | ❌ **未実施**（アカウントが無い） |
| n8n ワークフロー実行 | 構造検証は CI。**ランタイム動作は CI で検証していない** |
| Anthropic / OpenAI 実呼び出し | ❌ **未実施**（スタブトランスポート経由のみ） |
| docker compose | ❌ **未実行**（Docker デーモンが無い環境で作成） |

---

## 独立レビュー

- 2026-08-29 起動（`independent-review`・盲検・入力選定も委譲）
- **結果は本節に追記する。未追記＝未完了。**

---

## 残作業（本人作業）

1. GitHub リポジトリ `yuten0901/ghl-n8n-lead-automation` を作成し push
2. push 後に CI が緑になることを確認（ローカルと CI は別物）
3. n8n に実インポートして手動受け入れ（工程6相当）
4. プロフィール README を `yuten0901/yuten0901` に作成

---

## 教訓

- **ドキュメントの数字は書いた瞬間から腐る。** 同じセッション中に order-sync-service で
  「303 テスト」表記が実測 366 とずれているのを発見した。本リポジトリでは
  `tests/unit/test_documentation.py` が README の主張を実測と突き合わせる。
  order-sync-service にも同種の検査を追加した。
- **非同期サービスに同期 DB ドライバを混ぜると、性能でなく正しさが壊れる。**
  同時配信テストがデッドロックして初めて分かった（`docs/architecture.md` に記録）。
- **チェッカは「落ちること」を確かめて初めてチェッカになる。** 秘密スキャナは
  CI で偽の鍵を植えて非ゼロ終了を要求している。
